// Placeholder committed to source control on purpose - the real values are injected at deploy
// time from a GitHub Actions secret (AI_BOT_CONFIG_JS) and are never written into this repository
// or its history. See .github/workflows/deploy-pages.yml.
export const AI_BOT_CONFIG = {
  apiKey: "REPLACED_AT_DEPLOY_TIME",
  systemPrompt: "REPLACED_AT_DEPLOY_TIME",
  model: "REPLACED_AT_DEPLOY_TIME",
  voiceName: "REPLACED_AT_DEPLOY_TIME",
};
